# torch 运行时依赖。init_task_runtime.ps1 使用 --index-url
# https://download.pytorch.org/whl/cu128（CUDA）或 /cpu（CPU）安装。
# Irodori-TTS 上游要求 torch>=2.10.0、torchcodec>=0.10.0。
torch>=2.10.0,<2.11.0
torchaudio>=2.10.0,<2.11.0
torchcodec>=0.10.0,<0.11.0
torchvision
