# 基础依赖（init_task_runtime 在 torch 安装前执行本文件，故不得包含依赖 torch 的包）。
# 版本对齐上游 index-tts pyproject.toml（2026-09 快照），裁掉全仓零引用的
# keras / opencv-python / pandas 与构建工具 cython；torch 系见 requirements-torch.txt，
# 依赖 torch 的包（accelerate / descript-audiotools / openai-whisper）见
# requirements-post-torch.txt。
# 注：上游 requires-python <3.12 仅约束其自身包元数据，本适配器经 sys.path 导入
# 不受限制；所列版本均有 cp312 轮子，conda_env 的 python=3.12 可用。
cn2an==0.5.22
einops>=0.8.1
ffmpeg-python==0.2.0
fugashi>=1.2.0
unidic-lite>=1.0.0
g2p-en==2.1.0
jieba==0.42.1
json5==0.10.0
librosa==0.10.2.post1
matplotlib==3.10.0
modelscope==1.27.0
munch==4.0.0
numba==0.63.0
numpy==2.2.6
omegaconf>=2.3.0
safetensors==0.5.2
sentencepiece>=0.2.1
tensorboard==2.20.0
textstat>=0.7.10
tokenizers==0.21.0
requests>=2.28
tqdm>=4.67.1
transformers==4.52.1
huggingface_hub>=0.30.0
hf_xet>=1.0.0
wetext>=0.0.9; sys_platform != 'linux'
WeTextProcessing; sys_platform == 'linux'
